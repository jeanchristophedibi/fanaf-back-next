'use client';
import React, { useState } from 'react';
import Link from 'next/link';
import { Button } from '../ui/button';
import { Input } from '../ui/input';
import { Label } from '../ui/label';
import { Eye, EyeOff } from 'lucide-react';
import AuthLayout from './AuthLayout';
import loginImage from '../../assets/images/img-1.jpg';
import Image from 'next/image';

export default function SignInForm() {
  const [showPassword, setShowPassword] = useState(false);

  return (
    <AuthLayout image={loginImage} title="Bienvenue" subtitle="Connectez-vous pour accéder à votre espace">
      <div className="w-full max-w-6xl mx-auto">
        <div className="grid grid-cols-1 md:grid-cols-2 gap-8">          
          {/* Section Gauche */}
          <div className="relative h-full min-h-[600px] rounded-lg overflow-hidden">
            <div className="mb-6">
              <Image src={loginImage} alt="FANAF 2026" width={500} height={500} className="rounded-lg shadow-lg w-full h-auto object-cover" />
            </div>
            <div>
              <h1 className="mb-2 font-semibold text-3xl text-gray-800">Connexion</h1>
              <p className="text-sm text-gray-500">Entrez votre email et mot de passe pour vous connecter !</p>
            </div>
            
            <div className="space-y-3">
              <div className="flex items-start">
                <div className="w-6 h-6 rounded-full bg-orange-600 flex items-center justify-center text-white text-sm font-bold mr-3">✓</div>
                <p className="text-sm text-gray-600">Accès sécurisé à votre espace</p>
              </div>
              <div className="flex items-start">
                <div className="w-6 h-6 rounded-full bg-orange-600 flex items-center justify-center text-white text-sm font-bold mr-3">✓</div>
                <p className="text-sm text-gray-600">Gestion complète de l'événement</p>
              </div>
              <div className="flex items-start">
                <div className="w-6 h-6 rounded-full bg-orange-600 flex items-center justify-center text-white text-sm font-bold mr-3">✓</div>
                <p className="text-sm text-gray-600">Tableau de bord en temps réel</p>
              </div>
            </div>
          </div>

          {/* Section Droite */}
          <div className="flex flex-col justify-center">
            <div className="grid grid-cols-1 gap-3 sm:grid-cols-2 sm:gap-5 mb-6">
              <Button variant="outline" className="w-full">Se connecter avec Google</Button>
              <Button variant="outline" className="w-full">Se connecter avec X</Button>
            </div>

            <div className="relative py-3 sm:py-5">
              <div className="absolute inset-0 flex items-center">
                <div className="w-full border-t border-gray-200"></div>
              </div>
              <div className="relative flex justify-center text-sm">
                <span className="p-2 text-gray-400 bg-white px-5 py-2">Ou</span>
              </div>
            </div>

            <form>
              <div className="space-y-5">
                <div>
                  <Label htmlFor="email">Email</Label>
                  <Input type="email" id="email" placeholder="Entrez votre email" />
                </div>
                <div>
                  <Label htmlFor="password">Mot de passe</Label>
                  <div className="relative">
                    <Input placeholder="Entrez votre mot de passe" type={showPassword ? "text" : "password"} id="password" />
                    <button type="button" onClick={() => setShowPassword(!showPassword)} className="absolute right-3 top-1/2 -translate-y-1/2 text-gray-400 hover:text-gray-600">
                      {showPassword ? <EyeOff className="w-4 h-4" /> : <Eye className="w-4 h-4" />}
                    </button>
                  </div>
                </div>
                <Button type="submit" className="w-full bg-orange-600 hover:bg-orange-700">
                  Se connecter
                </Button>
              </div>
            </form>

            <div className="mt-5">
              <p className="text-sm font-normal text-center text-gray-700">
                Vous n'avez pas de compte ?{' '}
                <Link href="/signup" className="text-orange-600 hover:text-orange-700 font-medium">
                  S'inscrire
                </Link>
              </p>
            </div>
          </div>

        </div>
      </div>
    </AuthLayout>
  );
}